services:
  - type: web
    name: finance-platform
    runtime: node
    rootDir: backend
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: NODE_VERSION
        value: "18"
    plan: free
