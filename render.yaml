services:
  - type: web
    name: finance-platform
    runtime: python
    rootDir: backend
    buildCommand: "pip install --upgrade pip setuptools<81 wheel && pip install -r requirements.txt"
    startCommand: "python server.py"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
    plan: free