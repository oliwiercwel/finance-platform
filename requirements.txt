# Zależności dla projektu finance-platform
# Wersje dostosowane do Pythona 3.9.7

# Główne zależności
Flask==2.0.1
requests==2.26.0
python-dotenv==0.19.0

# Zależności dla API
yahoo-finance==1.4.0

# Zależności dla frontend
waitress==2.0.0

# Zależności dla bazy danych (jeśli potrzebne)
# sqlite3 jest wbudowany w Pythona
# psycopg2==2.9.1  # Odkomentuj, jeśli używasz PostgreSQL

# Zależności dla testów
pytest==6.2.4